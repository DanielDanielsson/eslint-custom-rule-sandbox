/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable no-unused-vars */

const foo = () => {};

const bar = () => {};

const fooBar = () => {};

// foo();
// bar();
fooBar();

const myObjext = {
  d: 'Hello',
  b: 'World',
  c: '!',
};
interface MyInterface {
  a: string;
  b: string;
  c: string;
  d: string;
}

type MyType = {
  a: string;
  b: string;
  c: string;
};

enum MyEnum {
  a = 'a',
  b = 'b',
  c = 'c',
}

/* eslint-disable @typescript-eslint/no-unused-vars */
/* eslint-disable no-unused-vars */
const foo = () => {};

const bar = () => {};

const fooBar = () => {};

// foo();
// bar();
fooBar();

const myObjext = {
  d: 'Hello',
  b: 'World',
  c: '!',
};
interface MyInterface {
  a: string;
  b: string;
  c: string;
}
